{% set name = "repoze.who" %}
{% set version = "2.4.1" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: 8a4c9b9268b4ff0edd906e97c2eed7ce83eb9f62e4250bfe03bcdbba6d31a237

build:
  noarch: python
  number: 0
  script: {{ PYTHON }} -m pip install . -vv

requirements:
  host:
    - pip
    - python
    - setuptools
  run:
    - python
    - setuptools
    - webob
    - zope.interface

test:
  imports:
    - repoze
    - repoze.who
    - repoze.who.plugins
    - repoze.who.plugins.tests
    - repoze.who.plugins.tests.fixtures
    - repoze.who.tests
  requires:
    - webob
    - zope.interface
    - nose
    - coverage

about:
  home: http://www.repoze.org
  license: BSD-3-Clause
  license_family: BSD
  license_file: LICENSE.txt
  summary: repoze.who is an identification and authentication framework for WSGI.
  doc_url: https://pypi.org/project/repoze.who/
  dev_url: https://github.com/repoze/repoze.who

extra:
  recipe-maintainers:
    - mxr-conda
    - mwilson8
    - rluria14
    - oblute
